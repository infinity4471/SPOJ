#include <cstdio>
#include <algorithm>

using namespace std;

int main( void )
{
	freopen("test.in","wt",stdout);
	printf("200000\n");
	for( int i = 0; i < 100000; i++ ) {
		printf("I %d\n", -1000000000 + i);
	}
	for( int i = 1; i <= 100000; i++ ) {
		printf("K %d\n", i );
	}
	return 0;
}
