#include <cstdio>

int main( void )
{
	int T;
	scanf("%d",&T);
	while( T-- ) {
		int N;
		scanf("%d",&N);
		long long ans = 0;
		for( int i = 0; i < N; i++ ) {
			if( i % 3 == 0 || i % 5 == 0 ) {
				ans += i;
			}
		}
		printf("%lld\n", ans );
	}
	return 0;
}
