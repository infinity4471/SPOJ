#include <fstream>
#include <iostream>

using namespace std;

int main( void )
{
	ifstream pfin1("test.out");
	int val[ 200000], i = 0;
	while( pfin1 >> val[ i++ ] != EOF );
	pfin1.close();
	ifstream pfin("test1.out");
	for( int j = 0; j < i; j++ ) {
		int value;
		pfin >> value;
		if( value != val[ j ] ) {
			cout << "line " << j + 1 << " expected " << val[ j ] << " got " << value << endl;
		}
	}
	return 0;
}
